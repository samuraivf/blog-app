import * as liked from '../icons/heart.png'
import * as notLiked from '../icons/heart-not-hover.png'

import * as notSaved from '../icons/save-instagram.png'
import * as saved from '../icons/save-instagram-hover.png'

import * as editIcon from '../icons/pencil.png'
import * as editIconHover from '../icons/pencil-hover.png'

export const LikeIcons = {
    liked: liked.default,
    notLiked: notLiked.default
}

export const SaveIcons = {
    saved: saved.default,
    notSaved: notSaved.default
}

export const EditIcons = {
    default: editIcon.default,
    hover: editIconHover.default
}